# GodotPRDemos

This repo was created to deal with the ever growing number of proof-of-concept
demos created for Godot pull requests.

Each branch is a separate demo unless specified otherwise in the branch README.

If you met an error related to git submodules when changing between branches locally,
first, delete folders inside `.git/modules/` and then try to init submodules again.